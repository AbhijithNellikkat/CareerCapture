import 'package:flutter/material.dart';

class CreateNewPdfView extends StatelessWidget {
  const CreateNewPdfView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
